{
  "name": "markhamrealtors-directory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && npx @tailwindcss/cli -i ./src/input.css -o ./dist/styles.css --minify && cp index.html realtors.json robots.txt sitemap.xml dist/"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
